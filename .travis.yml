language : node_js
node_js : 
    - "0.10"
before_install : 
    - npm install -g grunt-cli 
    - npm install -g coffee-script
    - npm install -g jade
    - npm install -g compass

install: npm install
# before_script: grunt minify